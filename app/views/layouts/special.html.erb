<!DOCTYPE html>
<html>
  <head>
    <title>Blogprojecttwo</title>
    <!-- Bootstrap Css Path -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if alert.present? %>
      <p class="alert alert alert-primary"><%= alert %></p>
    <% end %>
    <% if notice.present? %>
      <p class="notice alert alert-primary"><%= notice %></p>
    <% end %>
    <div class="from" style="padding-top: 10vh;">
      <div class="container shadow-lg bg-white rounded center d-flex">
        <div class="container .mt-4 d-flex ">
          <div class="">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
